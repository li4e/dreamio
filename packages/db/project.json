{
  "name": "db",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/db/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "migration:create": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenvx run --env-file=packages/firebase/environment/.secret.local -- npx prisma migrate dev --schema packages/db/src/schema.prisma --create-only"
      }
    },
    "migration:apply": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenvx run --env-file=packages/firebase/environment/.secret.local -- npx prisma migrate deploy --schema packages/db/src/schema.prisma && npm run postinstall && nx run db:migration:apply:test"
      }
    },
    "migration:apply:PROD": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenvx run --env-file=packages/db/.prod.env -- npx prisma migrate deploy --schema packages/db/src/schema.prisma"
      }
    },
    "migration:apply:test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotenvx run --env-file=packages/api/.test.env -- npx prisma migrate deploy --schema packages/db/src/schema.prisma"
      }
    },

    "prepare": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx prisma generate --schema packages/db/src/schema.prisma && npx ts-node packages/db/src/scripts/generateModelsIndex.ts"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/db/jest.config.ts"
      }
    }
  }
}
