{
  "name": "mobile-deploy",
  "version": "1.0.0",
  "scripts": {
    "deploy:ios": "bundle exec fastlane beta",
    "deploy:meta": "bundle exec fastlane update_meta",
    "deploy:screenshots": "bundle exec fastlane update_screenshots"
  }
}
