{
  "name": "dreamio",
  "version": "0.0.1",
  "author": "li4e",
  "workspaces": [
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "start": "npm run start -w apps/mobile",
    "ios": "npm run ios -w apps/mobile",
    "android": "npm run android -w apps/mobile",
    "prebuild": "dotenv -e .env -- npm run prebuild -w apps/mobile",
    "prebuild:screenshots": "npm run prebuild && npm run build -w tools/screenshots",
    "deploy:web": "firebase deploy --only hosting",
    "deploy:ios": "dotenv -e .env -- npm run prebuild && bundle exec fastlane beta",
    "deploy:meta": "dotenv -e .env -- bundle exec fastlane update_meta",
    "deploy:screenshots": "dotenv -e .env -- bundle exec fastlane update_screenshots",
    "screenshots": "npm run start -w tools/screenshots",
    "translate": "dotenv -e .env -- npm run translate -w tools/translator"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "dotenv-cli": "^8.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.4.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "patch-package": "^8.0.0"
  }
}
